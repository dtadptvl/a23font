task: T-005
contract_rev: 1
dispatch_id: 1
status: completed
proved: ["M3.A1","M3.A2","M3.A3","M3.A4"]
changed: ["pipeline/models.py","pipeline/cache.py","pipeline/discovery.py","tests/test_cache.py","tests/test_discovery.py"]
evidence: ["M3.A3 pytest tests/test_cache.py green tail: 13 passed, 1 warning in 0.19s - options segregation (distinct dirs, cross-options miss, tampered stored-options miss), pipeline_version 1 vs 2 gating miss/binary, schema_version 999 miss, validation gating passed=False not binary then passed=True binary with sha256 of both binaries in meta, fontmodel-level hit without binaries, partial hit after 3-glyph checkpoint + load_frozen_glyphs resume, crash resilience (torn meta.json.tmp.CRASH left behind while meta.json byte-identical and lookup still partial), zero .tmp residue after atomic writes, corrupt frozen.jsonl line skipped with valid lines kept, cumulative checkpoint rewrite 5 then 20 glyphs (20 lines, meta.frozen_glyphs=20), invalidate sets failed+failure reason with lookup miss and artifacts retained, CHECKPOINT_BATCH==16, dir_size/total_size recursive counts","M3.A4 pytest tests/test_discovery.py green tail: 14 passed, 1 warning in 0.07s - stop rules covered: short_page (100+37=137 glyphs, coverage sorted), empty_layout (incl. missing layout key), repeated_signature, no_new_glyphs (same glyphs under different keys/image), error (fetch exception + non-dict payload, message recorded in notes), max_pages budget of 3; defensive parsing (lowercase codepoint honored, unicode str coerced to int, layout-key gid fallback, 500-char bitmap and nested dict skipped, non-dict layout value skipped), cp<=0 excluded from unicode_coverage, page_delay sleep only between full pages; gmap_page_url exact expected string, glyph_raster_url rt=A / rt=%E1%BB%AF (Vietnamese u-hook) / rt=A%20B with md5 path segment, sc=2 default, w=2*size default, render_mode=new; module source free of httpx/requests/aiohttp/socket imports","M3.A1/M3.A2 evidence alive: full offline suite tail: 134 passed, 1 deselected, 1 warning in 14.99s via pytest -m 'not live' -o addopts= (pre-T-005 baseline 107 + 13 cache + 14 discovery; only pre-existing starlette deprecation warning remains)","cache hit proves all five fast15 conditions: source identity via identity_key over SourceIdentity.stable_id/raw md5 identity, version compatibility via CACHE_SCHEMA_VERSION=1 + pipeline_version equality in meta.json, successful build state via meta.status=success, validation state via validation_passed + report.json, relevant options via options_key dir suffix + stored options equality; every write is <path>.tmp.<rand> + os.replace with parent mkdirs and tmp cleanup on failure; no fsync per glyph, cumulative glyphs/frozen.jsonl rewrite per checkpoint batch","discovery performs zero network I/O: fetch_page is an injected async callable (url)->dict; RASTER_HOST sig.monotype.com gmap/raster URL builders are pure string functions (asyncio imported only for the optional inter-page sleep)"]
failure_class: null
failure_boundary: null
note: GlyphManifest extended compatibly - existing fields md5/total_glyphs/unicode_coverage/pages/stop_reason/glyphs unchanged in order, entries (gid/cp/meta dicts) and notes appended as trailing default_factory fields, so keyword construction in tests/test_myfonts_adapter.py (incl. manifest.glyphs == [] assertion) still passes, proved by the full suite. All five files written via PowerShell single-quoted here-strings, LF-normalized, UTF-8 without BOM; verified first three bytes of each file are 22 22 22 (opening triple-quote) and not EF BB BF. .prime/state.yaml and task.yaml diffs in git status are ctl dispatch transitions (draft to active, dispatch_id 0 to 1), not worker edits; excluded pipeline files (source_myfonts, geometry_core/fit/check, build, validate) untouched.