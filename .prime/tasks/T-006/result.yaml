task: T-006
contract_rev: 1
dispatch_id: 1
status: completed
proved: ["M5.A2","M5.A3"]
changed: ["pipeline/raster.py","pipeline/metrics.py","pipeline/orchestrator.py","pipeline/orchestrator_refine.py","app/db.py","app/services.py","app/config.py","worker/main.py","tests/test_orchestrator.py","tests/test_worker_resume.py",".env.example"]
evidence: ["M5.A2 pytest tests/test_orchestrator.py tests/test_worker_resume.py -o addopts= -v tail: 9 passed, 1 warning in 43.53s - all PASSED incl. resume/cancel proofs: test_crash_after_first_freeze_then_resume_without_reacquire (interrupt hook raises after 1st freeze -> cumulative checkpoint persists -> cache lookup partial with frozen_glyphs>=1 -> rerun completes 3 frozen, provider counter proves zero acquisitions for any frozen gid on resume), test_worker_persistence_restart_and_requeue (QUEUED job claimed, synthetic runner to DONE, jobs row DONE styles_total/done 1, stale-heartbeat simulation requeued by requeue_stale_jobs -> QUEUED stage NULL worker NULL attempts+1 with requeued_stale event, fresh-heartbeat second call requeues 0, rerun DONE with style rows preserved - no duplicates - and cache_hit=1), test_cancel_between_styles_ends_cancelled (request_cancel after style A -> between-styles check raises CancelledWork -> job CANCELLED, style A DONE style B QUEUED, cancel_requested+finished events), plus test_offline_reconstruction_freezes_all_glyphs, test_second_run_is_binary_cache_hit_with_zero_acquisitions (provider.calls == {}), test_fontmodel_cache_route_rebuilds_binaries, test_budget_exceeded_still_finalizes_fast_lane_glyphs (deadline in past -> report budget_exceeded true, fast-lane glyphs still freeze), test_vietnamese_option_raises_honest_pending_error, test_default_runner_fails_network_disabled_when_offline","M5.A3 report_json captured from the resume-scenario worker run (job DONE): duration_s 6.022, max_rss_kb 580420, mem_available_kb null (dev host is Windows - no /proc/meminfo; key recorded with null as the honest value, populated on the Linux A23), platform Windows, rss_note resource.getrusage ru_maxrss normalized to KB (bytes on Windows; stdlib ctypes GetProcessMemoryInfo fallback where the resource module is absent), styles[0] name Synthetic Regular status DONE duration_ms 6021 cache_hit false; test_worker_persistence_restart_and_requeue asserts max_rss_kb>0, duration_s>=0.0, mem_available_kb key present, and resources are also recorded on the CANCELLED path","FULL suite: .venv python -m pytest -m not live -o addopts= -q tail: 143 passed, 1 deselected, 1 warning in 57.93s (pre-T-006 baseline 134 + 9 new M5 tests; only pre-existing starlette deprecation warning remains)"]
failure_class: null
failure_boundary: null
note: Honest milestone gaps wired explicitly, never faked - options.vietnamese raises PipelineNotImplementedError (worker maps to VIETNAMESE_PENDING), BrowserMetricsProvider.measure_batch raises NotImplementedError (browser metrics milestone pending, heuristic estimator used), kerning/features skipped with note typography inference milestone pending, default worker runner guards all live network behind A23FONT_PIPELINE_LIVE (env default false in this milestone, documented in worker docstring and .env.example; T-007 enables on A23) failing claimed jobs with NETWORK_DISABLED otherwise. Schema migration v2 idempotent (worker_heartbeat column + SCHEMA_VERSION 2, verified against a hand-built v1 database). No new third-party deps (stdlib ctypes for Windows peak RSS). Orchestrator stays at 459 lines; geometry integration fixes live only in the new orchestrator_refine.py (excluded geometry modules untouched - verified via git status) - root causes fixed without weakening any confidence threshold: bounded fit input via arc-length resample (simplify_closed is quadratic), corner-preserving anchors with windowed-angle detection and min-gap de-clustering (marching-squares staircase corners), corner-aware staircase smoothing, simplify-drift verification guard (greedy one-point removal measured drifting up to 175px on staircase loops), capped evidence polylines bounding cubic_residual memory, and a fixed point-ordering bug in the corner-interval resampler. All files written via PowerShell single-quoted here-strings, LF-normalized, UTF-8 without BOM (first three bytes verified 22 22 22 / 23 20 41, not EF BB BF). .prime/state.yaml diff is the ctl dispatch transition, not a worker edit. No git commit performed.