task: T-004
contract_rev: 2
dispatch_id: 1
status: completed
proved: ["M3.A1","M3.A2"]
changed: ["pipeline/geometry_core.py","pipeline/geometry_fit.py","pipeline/geometry_check.py","pipeline/geometry.py","pipeline/fontmodel.py","pipeline/build.py","pipeline/validate.py","tests/geometry_fixtures.py","tests/test_geometry.py","tests/test_fontmodel.py","tests/test_build_validate.py"]
evidence: ["M3.A1 pytest tests/test_geometry.py green 21 passed: circle fit_closed_cubic 14 segments exact closure+continuity residual 0.8987px<=1.5px at 1024 (Schneider 2x2 least-squares actually solved; alpha converges to theoretical 165.6 on R300 quarter arc); extract_contours circle perimeter error 0.22% signed area positive; ring 2 loops areas +282957/-70790 (outer CCW hole CW); rasterize_glyph(fitted circle) IoU vs original mask 0.9977>=0.90; confidence_checks circle PASS metrics edge_agreement 0.9108 components 1 holes 0 self_intersections 0 bezier_residual 0.8965<=2x0.9766","M3.A1 mutation failure codes exact: open contour -> [not_closed]; NaN coord -> [non_finite, degenerate_segment]; advance 0 -> [advance_range]; topology mismatch -> [component_count, hole_count]; space glyph advance rule PASS","M3.A2 pytest tests/test_build_validate.py green 12 passed: fonttools_validate TTF passed=True errors=[] glyph_count=4 cmap_entries=3 tables head/hhea/maxp/OS2/name/cmap/post present; fonttools_validate OTF passed=True errors=[] glyph_count=4; heavy_validation passed=True skipped_engines=[] (uharfbuzz+freetype-py installed and exercised on dev host)","M3.A2 harfbuzz_validate: all cmap-present corpus codepoints (CORPUS_BASE fast15 corpus) shape to real glyphs gid!=0; freetype_validate 32/32 ink chars rasterized non-empty at 32px; cmap has 0x4F->uni004F and 0x48->uni0048 in both binaries; TTF has glyf and no CFF, OTF has CFF and no glyf; hmtx lsb==actual sampled xMin (204 for ring) space=(250,0)","full offline suite tail: 107 passed, 1 deselected, 1 warning in 16.80s via pytest -m 'not live' -o addopts= (pre-T-004 baseline was 65 passed; only pre-existing starlette deprecation warning remains)","zero new dependencies beyond requirements.txt (numpy/pillow/fonttools/uharfbuzz/freetype-py already listed); myfonts sources untouched (read-only reference)"]
failure_class: null
failure_boundary: null
note: uharfbuzz and freetype-py both installed into .venv (Python 3.14.6) and fully exercised - no engine skips in heavy_validation. geometry split into geometry_core/geometry_fit/geometry_check with geometry.py re-export surface is explicitly permitted by the contract (keep within pipeline/**). Algorithm fixes vs old engine (all measured, no threshold weakening): (1) Schneider 2x2 least-squares alpha solve actually implemented - old code built A/b but never solved (needed (b0+b1)P0+(b2+b3)P3 baseline + negated T2 column); (2) corner-free closed-loop fit split at even points with wrap tangents (single closed arc is degenerate start==end); (3) residual/intersection sampling density-adapted to segment length (max_step 0.5) so point-to-curve distance is not dominated by sample gaps; (4) hmtx=(advance,xMin) from actual sampled contour bbox with 0 fallback for empty glyphs, replacing hardcoded lsb; (5) chamfer backward sweep index fix (subtract idx3 not reversed); (6) rasterizer samples pixel centers on both axes + confidence edge band radius 2 so sub-half-pixel grid discretization between equivalent-scale renders does not dominate the agreement signal. pytest.ini addopts=-q combined with CLI -q yields -qq which suppresses the final summary line; tails quoted here were captured with -o addopts= (faithful single -q). geometry_core.py is 452 lines (~450 soft cap, left as-is per dispatch).
